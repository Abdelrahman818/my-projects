function len(param){let i=0;while(true){if(param[i]===undefined)break;i++;}return i;}function strRepeat(str,repeates,isSpace=false,spaceType=" "){if(typeof str!=="string")throw"You can only repeat a string";else if(typeof repeates!=="number")throw"Number of repeats must be integer";else if(typeof isSpace!=="boolean")throw"Number of repeats must be boolean value";let repeated="";if(isSpace){for(let i=0;i<repeates;i++){if(i+1===repeates){repeated+=str}else{repeated+=str+spaceType}}}else{for(let i=0;i<repeates;i++)repeated+=str}return repeated}function reverse(data){if(typeof data==="number")data=`${data}`;let rev="";for(let i=len(data)-1;i>=0;i--){rev+=data[i]}return rev}function createElement(elementName,attr,val="",inSideElement=""){let el;el=document.createElement(elementName);if(attr)el.setAttribute(attr,val);el.innerHTML=inSideElement;return el}function toArray(data){let arr=[];for(let i=0;i<len(data);i++){arr.push(data[i])}return arr}export{len,strRepeat,reverse,createElement,toArray}
